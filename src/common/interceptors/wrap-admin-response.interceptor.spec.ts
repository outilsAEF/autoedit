import { WrapAdminResponseInterceptor } from './wrap-admin-response.interceptor';

describe('WrapAdminResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapAdminResponseInterceptor()).toBeDefined();
  });
});
