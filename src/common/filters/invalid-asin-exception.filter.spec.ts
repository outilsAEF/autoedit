import { InvalidAsinExceptionFilter } from './invalid-asin-exception.filter';

describe('InvalidAsinExceptionFilter', () => {
  it('should be defined', () => {
    expect(new InvalidAsinExceptionFilter()).toBeDefined();
  });
});
